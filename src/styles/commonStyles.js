import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  maintext: {
    fontSize: 16,
    margin: 20,
  },
  barcodebox: {
    height: 200,
    width: 380,
    overflow: 'hidden',
    borderRadius: 10,
    backgroundColor: 'tomato',
    paddingTop: 0,
  },
});
